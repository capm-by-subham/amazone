_schema-version: 3.3.0
ID: delivery_management_system
description: A simple CAP project.
version: 1.0.0
modules:
- name: delivery_management_system-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: delivery_management_system-auth
  - name: delivery_management_system-db
  - name: delivery_management_system-destination
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: delivery_management_system-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: delivery_management_system-db
    properties:
      TARGET_CONTAINER: ~{hdi-container-name}
  parameters:
    buildpack: nodejs_buildpack
- name: delivery_management_system
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: delivery_management_system-auth
  - name: delivery_management_system-html5-runtime
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: delivery_management_system-app-deployer
  type: com.sap.application.content
  path: gen
  requires:
  - name: delivery_management_system-auth
  - name: srv-api
  - name: delivery_management_system-html5-repo-host
    parameters:
      config:
        HTML5Runtime_enabled: true
      content-target: true
  parameters:
    config:
      destinations:
      - Authentication: NoAuthentication
        HTML5.DynamicDestination: true
        HTML5.ForwardAuthToken: true
        Name: srv-api
        ProxyType: Internet
        Type: HTTP
        URL: ~{srv-api/srv-url}
  build-parameters:
    build-result: app/
    requires:
    - artifacts:
      - customer.zip
      name: delivery_management_systemcustomer
      target-path: app/
    - artifacts:
      - drivers.zip
      name: delivery_management_systemdrivers
      target-path: app/
- name: delivery_management_systemcustomer
  type: html5
  path: app/customer
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm ci
    - npm run build
    supported-platforms: []
- name: delivery_management_systemdrivers
  type: html5
  path: app/drivers
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm ci
    - npm run build
    supported-platforms: []
- name: delivery_management_system-destinations
  type: com.sap.application.content
  requires:
  - name: delivery_management_system-auth
    parameters:
      service-key:
        name: delivery_management_system-auth-key
  - name: delivery_management_system-html5-repo-host
    parameters:
      service-key:
        name: delivery_management_system-html5-repo-host-key
  - name: srv-api
  - name: delivery_management_system-destination
    parameters:
      content-target: true
  parameters:
    content:
      instance:
        destinations:
        - Name: delivery_management_system-html5-repository
          ServiceInstanceName: delivery_management_system-html5-repo-host
          ServiceKeyName: delivery_management_system-html5-repo-host-key
          sap.cloud.service: delivery_management_system.service
        - Authentication: OAuth2UserTokenExchange
          Name: delivery_management_system-auth
          ServiceInstanceName: delivery_management_system-auth
          ServiceKeyName: delivery_management_system-auth-key
          sap.cloud.service: delivery_management_system.service
        existing_destinations_policy: update
  build-parameters:
    no-source: true
resources:
- name: delivery_management_system-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
        redirect-uris:
        - https://*~{app-api/app-uri}/**
      role-collections:
      - description: generated
        name: customer (delivery_management_system ${org}-${space})
        role-template-references:
        - $XSAPPNAME.customer
      - description: generated
        name: driver (delivery_management_system ${org}-${space})
        role-template-references:
        - $XSAPPNAME.driver
      tenant-mode: dedicated
      xsappname: delivery_management_system-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: delivery_management_system-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
  properties:
    hdi-container-name: ${service-name}
- name: delivery_management_system-destination
  type: org.cloudfoundry.managed-service
  parameters:
    service: destination
    service-plan: lite
  requires:
  - name: srv-api
- name: delivery_management_system-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: delivery_management_system-html5-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: cross-container-service-1
  type: org.cloudfoundry.existing-service
  parameters:
    service-name: mid_night_crave-db
  properties:
    the-service-name: ${service-name}
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
